eureka:
  instance:
    hostname: ${CHI_DISCOVERY_HOST:}
    prefer-ip-address: false
    nonSecurePortEnabled: true
    securePortEnabled: false
  server:
    wait-time-in-ms-when-sync-empty: 0
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url:
      defaultZone: ${EUREKA_SERVICE_URL:}